# HTTP Monitor

The library module. Its usage guide — installing, instrumenting OkHttp and
`HttpURLConnection`, filtering, dashboard reporting, the API reference and the release
process — lives in one place at the repo root: [../README.md](../README.md).
